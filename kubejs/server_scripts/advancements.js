onEvent('player.advancement', function (event) {
  // Note: Currently disabled until (if) LevelZ adds a way to add points instead of just setting total points.
  //event.player.tell(event.advancement.id())
  //event.server.runCommandSilent(`/playerstats ${event.player.name} points 1`)
})